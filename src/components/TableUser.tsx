
export interface props {
    id: string;
    name: string;
    img: string;
    group: string
}
export default function TableUser ({id, name, img, group}:props) {
    return (
        <tbody>

        </tbody>
    )
}